# Skip configuration for SkipLTCTests module

# the blocks to add to the build.gradle.kts
#build:
#  contents:
#    - block: 'dependencies'
#      contents:
#        - '// no new dependencies in SkipLTC'
